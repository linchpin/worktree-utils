{
  "name": "@linchpin/worktree-utils",
  "version": "1.0.3",
  "description": "Linchpin worktree CLI for WordPress plugin review workflows",
  "license": "MIT",
  "type": "commonjs",
  "bin": {
    "linchpin": "bin/linchpin.js"
  },
  "files": [
    "bin",
    "src"
  ],
  "scripts": {
    "test": "node --test",
    "prepare": "husky || true",
    "commitlint": "commitlint --edit"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.4.1",
    "@commitlint/config-conventional": "^20.4.1",
    "husky": "^9.1.7"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "git",
    "worktree",
    "wordpress",
    "linchpin"
  ]
}
